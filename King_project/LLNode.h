#ifndef LL_NODE_H
#define LL_NODE_H

#include "BstNode.h"

class LLNode {
	public:						// Making all the data members as public is very exceptional.
		LLNode* next;				// pointer to next node	// access of data members
		BstNode* data;				// Since this facilitates rapid
};

#endif
